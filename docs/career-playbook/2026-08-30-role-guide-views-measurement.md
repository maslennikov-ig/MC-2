# Насколько хорош раскрой Role Guide по адресатам

Замер 2026-08-30 на 14 сохранённых завершённых плейбуках (9 русских, 5 английских) — всех, у которых
есть все 27 блоков. Только чтение: ни одного платного вызова.

Воспроизвести:

```bash
cd /home/me/code/mc2/packages/course-gen-platform
set -a; . .env; set +a
TMPDIR=/tmp pnpm exec tsx scripts/measure-role-guide-views.ts
```

## Что здесь считается качеством

Представление — это отдельно читаемый документ. Вопрос не «отфильтровали ли мы блоки»: каталог
отвечает на это по построению. Вопрос — «держится ли то, что получил читатель, само по себе».
Измеримая форма этого вопроса — перекрёстная ссылка. Блок, который говорит «см. Block 5», внутри
представления без блока 5 отправляет читателя на страницу, которую ему не выдали.

## Раскрой каталога

27 блоков, ни одного вне представлений, 5 общих для всех троих: `header`, `block_1` (миссия),
`block_14` (онбординг), `block_24` (Role Canvas), `block_25` (футер).

Эксклюзивные блоки: `block_9` Human-AI и `block_22` Role README — только сотруднику; `block_12`
профиль кандидата — только HR; `block_21` режимы отказа и `block_23` план преемственности — только
руководителю.

## Объём

| Представление | Блоков | Медиана знаков | Доля документа |
| ------------- | -----: | -------------: | -------------: |
| employee      |     20 |         51 544 |            72% |
| manager       |     20 |         52 427 |            74% |
| hr            |     14 |         33 249 |            47% |
| весь документ |     27 |         71 238 |           100% |

Сотрудник и руководитель получают примерно три четверти документа, HR — меньше половины.

## Ссылки на блок, которого читателю не дали

| Представление | Ссылок | Битых | Доля | Плейбуков затронуто |
| ------------- | -----: | ----: | ---: | ------------------: |
| employee      |    205 |    22 |  11% |                7/14 |
| manager       |    317 |    61 |  19% |               14/14 |
| hr            |    187 |   134 |  72% |               13/14 |

HR-представление — не «чуть хуже», а сломанное: семь ссылок из десяти ведут в никуда. Затронут
каждый плейбук в выборке.

Первая версия этого замера давала 150/235/141 ссылок: её собственный регекс искал `\bблок`, а `\b` —
граница ASCII-слова и на кириллице не срабатывает. То есть русские «блок 5» и «блоке 22» не
считались вообще. Это тот же дефект, который в тот же день нашли в правиле ритмов (mc2-539pz) и в
фильтре строки-заголовка матрицы решений. Цифры выше — после исправления.

## Куда именно ведут битые ссылки

| Цель       | Что это              | Её читатели      | Битых ссылок |
| ---------- | -------------------- | ---------------- | -----------: |
| `block_23` | План преемственности | manager          |           45 |
| `block_22` | Role README          | employee         |           39 |
| `block_5`  | Матрица решений      | employee+manager |           38 |
| `block_9`  | Человек и AI         | employee         |           28 |
| `block_11` | Карьерный трек       | employee+hr      |           10 |
| `block_6`  | KPI и метрики        | employee+manager |            9 |

Дальше хвост из единичных случаев по ещё пятнадцати блокам.

Что снимет добавление одного блока:

- employee: 22 битых, `block_23` снимает 12 (55%);
- manager: 61 битая, `block_22` снимает 20 (33%), `block_9` — 18 (30%), `block_11` — 10 (16%);
- hr: 134 битых, `block_5` снимает 38 (28%), `block_23` — 33 (25%), `block_22` — 19 (14%).

## Почему так вышло

Это не случайные промахи модели, а заложенное противоречие. Групповой промпт содержит безусловное
правило:

> AUTHORITY — Block 5 is the single source of decision authority. If you mention a decision that
> appears in the digest, reference Block 5 and do not restate its approval level in your own words.

Правило верное: именно повторение уровня согласования своими словами и породило три разных
утверждения об одном решении в разборе от 2026-08-11. Но блок 5 не входит в HR-представление, а
`block_audiences_md` передаёт генератору читателей только его собственных блоков — ничто не говорит
ему «блок, который ты пишешь, читает HR, а блока 5 у HR нет». Промпт велит ссылаться, каталог
отбирает цель.

То же самое с `block_23` (план преемственности, только руководитель): на него ссылаются `block_24` и
`block_26`, которые видят все три читателя.

## Чего у HR нет вообще

`block_2` анти-цели, `block_3` зоны ответственности, `block_4` обязанности, `block_5` матрица
решений, `block_6` KPI, `block_9` человек и AI, `block_10` зависимости, `block_16` основной процесс,
`block_18` FAQ, `block_20` бизнес-модель, `block_21` режимы отказа, `block_22` README,
`block_23` преемственность.

При этом HR получает `block_24` Role Canvas и `block_26` чек-лист внедрения — сводные блоки, которые
по своей природе ссылаются на всё перечисленное.

## Ещё одно, отдельно от качества раскроя

`buildRoleGuideView` экспортирована и покрыта тестом, но **её никто не вызывает**: ни API, ни
фронтенд. Поиск по `packages/web` не даёт ни одного упоминания адресатов. Представления сегодня
существуют как каталог, функция и тест, но ни один читатель их не получает; продукт по-прежнему
отдаёт весь документ целиком.

Это меняет срочность, а не суть: пока представления не отданы наружу, битые ссылки никого не задели.
Как только их отдадут — задели бы сразу, и сильнее всего HR.

## Что сделано

Выбран путь «сделать правило ссылок адресатным» (решение владельца 2026-08-30, до платного прогона).

**Правило.** Блок может назвать другой блок, только если _каждый_ читатель первого читает и второй:
подмножество, а не пересечение. `block_26` (manager+hr) не может сослаться на `block_5`
(employee+manager), хотя руководитель читает оба — HR-читатель `block_26` блока 5 не получит.
`careerPlaybookBlockMayCite` в `nodes/audience-scope.ts`.

**Промпт.** Новая переменная `citable_blocks_md` перечисляет для каждого выходного блока группы, что
именно ему разрешено назвать. Раздел REFERENCES запрещает называть остальное в любой формулировке —
ни «Block 12», ни «раздел о профиле кандидата», ни «как описано в другом месте». Правило AUTHORITY
стало условным: ссылаться на блок 5, если он в списке; иначе привести уровень согласования дословно
как его даёт digest. Переписывание своими словами по-прежнему запрещено — именно оно породило три
разных утверждения об одном решении.

**Проверка.** Детерминированная `validateCrossViewReference`, категория `unreadable_reference`,
входит в критическую таксономию, то есть блок уходит на регенерацию. На 14 сохранённых плейбуках
срабатывает во всех 14: медиана 4 блока, максимум 16.

**Регенератор тоже получил список.** Иначе регенерация услышала бы «ссылка неверна», но не узнала бы,
какие ссылки верны, и сожгла бы обе попытки на воспроизведение того же дефекта. Заодно убрана третья
копия `getCareerPlaybookBlockAudiences`, которая жила прямо в регенераторе.

Это меняет генерацию, поэтому цифры выше — «до». Мерить «после» тем же скриптом на платном прогоне.

## Что остаётся выбором владельца

Двигать галочки (mc2-de3vu): `block_5` в HR снимает 28% битых ссылок ещё до всякой генерации, `block_23` в
employee — 55%. Это продуктовое решение о том, кому что читать, и оно не отменяется правилом выше:
правило заставляет блок быть самодостаточным, а галочка решает, надо ли читателю вообще видеть тот
материал. Отдельно стоит решить, что делать с `block_24` Role Canvas и `block_26` чек-листом: их
читают все трое, поэтому назвать они теперь могут только пять общих блоков и по природе своей станут
длиннее.
