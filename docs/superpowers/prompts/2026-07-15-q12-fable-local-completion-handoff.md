Target: Claude / Fable 5, root engineering orchestrator
Audience: Fable with `/home/me/code/mc2`

Goal:
Finish the remaining safe local work for epic `mc2-jz6y0`, starting with the
owner-approved D5J product-truth correction. Complete design amendment, plan,
TDD, independent review, integration, W/M/H/D6/Root joins, verification, docs,
Graphify refresh, Beads, commits and pushes. Stop before every remote/live Q12
action.

Success criteria:

- Decision `.13.22` becomes an independently reviewed normative amendment;
  `.13.21` then passes plan review, TDD, acceptance and integration without
  fabricated authority.
- W `.13.10` retains its two current edits, is completed/reviewed/integrated;
  M `.13.11` and H `.13.12` follow dependencies; D6 `.13.19` and Root `.13.13`
  complete locally.
- Required focused/recovery/isolation/Compose/Qdrant gates, `pnpm type-check`,
  `pnpm build` and process verification pass. Run canonical stage closeout only
  when its local readiness conditions are true; otherwise record the exact
  remote/live tail as an explicit defer and leave Q12 open.
- Every accepted branch and integration result is committed and pushed; the
  final report is in Russian with exact commits, totals, cleanup and remote gate.

Context:

- Start from current `origin/codex/self-hosted-qdrant-platform`; integration was
  clean/pushed at `038ed4c5` before this handoff. Use the existing D5J worktree
  `/home/me/code/mc2/.worktrees/q12-d5j-joined-fixture`, branch
  `codex/q12-d5j-joined-fixture`; it was clean/pushed at `e2c8bfbd` before the
  handoff sync. Resolve current origin SHAs instead of assuming these remain tips.
- Read `AGENTS.md`, `.codex/orchestrator.toml`, `.codex/handoff.md`,
  `.codex/project-index.md`, `graphify-out/GRAPH_REPORT.md`, stage summary,
  Beads `.13.22/.13.21/.13.10/.13.11/.13.12/.13.19/.13.13`, the D5J spec, and
  the two ignored audits:
  `.superpowers/sdd/q12-d5j-command-binding-audit.md` SHA-256
  `17f61bc5681a8d19f0a237c6e72aca7a4ed89fbbbe02c28712190a64cbd1148e` and
  `.superpowers/sdd/q12-d5j-plan-architecture-map.md` SHA-256
  `942b3423eea39c0fb08606eeb7ddbd32c2e4db11934b4d819f1c9b4898328469`.
- Owner approval is recorded in `.13.22`: proceed through all local amendment,
  plan, implementation, review and integration steps without intermediate
  confirmation requests. Codex will review Fable's completed work afterward.

Approved correction:

- Keep one canonical command authority. Move forward from Task 9 only the
  enumerated ordinary commands required by D5J; never add a second resolver.
- Freeze the exact phase-to-command/outcome/literal-argv/runtime-substitution
  table. Multi-command phases preserve their real lifecycle evidence. Do not use
  `root.advance`, zero hashes, W's `barrier.prepare-recovery`/`9*64`, or caller
  rows/hashes as positive authority.
- Root owns a deterministic closed fixture inventory; W supplies only its
  already-approved immutable quiesce bytes and validates Root output read-only.
- Freeze distinct, stable, immutable forward/rollback FWM paths so the
  activation-frontier rollback cannot overwrite the forward object.
- Task 9 later extends and reuses the same manifest/composer with byte/order
  parity. No deployed CLI fixture flag or weakened strict/recovery tests.

Execution constraints:

- Use `orchestrator-stage`, `task-router`, brainstorming/writing-plans, TDD,
  subagent-driven development, `test-pass`, verification-before-completion and
  orchestration-closeout. Publish a fresh decomposition matrix; use visible
  isolated subagents only when a coherent complex stream passes the complex
  material-benefit gate, and inspect all diffs/evidence.
- Preserve `/home/me/code/mc2/.worktrees/q12-w-writer-barrier` and exactly its
  two uncommitted files: `deploy/qdrant/q12-writer-resume.py` and
  `packages/course-gen-platform/tests/unit/ops/qdrant-source-recovery-runtime.test.ts`.
  Preserve unrelated `.claude/settings.json` changes.
- External S3 remains deferred. Never recover or mutate Qdrant Cloud. Never
  expose or reuse credentials in prompts, Git, argv or logs.
- No GHCR publication, SSH, Supabase/server/database/container/service/Qdrant
  mutation, deploy, live reindex, staging/production activation or credential
  rotation is authorized. Before that boundary, present exact effects, secrets,
  observation, rollback and downtime/data impact, then ask the owner.

Output:
Maintain stage artifacts, current handoff/summary and stable docs. Record
`docs-reviewed` and `graph-reviewed`; refresh Graphify locally only after an
accepted implementation boundary, with no external model/API mode or Git hooks.
Report accepted evidence, blockers/defers, branch/commit/push state, rollback
state and the exact remaining activation gate in Russian.

Stop:
Ask only at the remote/live boundary, unavailable required secret, a new genuine
product-truth gap not covered above, unisolatable ownership conflict, or a
repeatedly failing required gate after in-scope diagnosis. Otherwise continue.
