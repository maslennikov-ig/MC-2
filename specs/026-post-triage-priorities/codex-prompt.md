# Стартовый промпт для Codex (ChatGPT 5.6)

Вставить в свежую сессию Codex в `/home/me/code/mc2`. Всё остальное — в `spec.md`
рядом; промпт намеренно не пересказывает его.

---

Runtime: Codex CLI, ChatGPT 5.6.
Target: `/home/me/code/mc2`, ветка `develop`.
Audience: агент-исполнитель с правом менять код репозитория.

Goal: доделать оставшийся долг по `specs/026-post-triage-priorities/spec.md` в
заданном там порядке. Прочитай спецификацию первой, затем `AGENTS.md`,
`.codex/orchestrator.toml`, `.codex/handoff.md`,
`.codex/repository-failure-modes.md`.

Context: бэклог уже отревизован — 89 записей сверены с кодом, 27 закрыты с
коммитом или измерением, осталось 49. Не переоткрывай закрытые и не переранжируй:
порядок в спецификации выведен из улик, а приоритеты в трекере измеренно
ненадёжны в обе стороны. Начни с Tier 1 (§3).

Constraints:

- Reindex, миграции схемы, секреты, доступы, force-push — запрещены. Deploy —
  только на зелёном пайплайне.
- Закрытие требует коммита или измерения; «выглядит сделанным» не причина.
- `AGENTS.md` переписывается хуком `bd`: коммить явными путями, никогда
  `git add -A`.
- §9 спецификации — список того, что трогать нельзя. Соблюдай его буквально.

Verification: `pnpm type-check` и `pnpm build` на изменённое.
**Бэкенд-тесты сейчас не запускаются и выходят с кодом 0** — `global-setup.ts:20`
падает на проверке версии Qdrant и печатает «No test files found, exiting with
code 0». Пока `mc2-3sz3d` не починен, зелёный бэкенд ничего не доказывает.
Веб-тесты работают.

Output: по каждой закрытой задаче — что изменилось, чем доказано, какой командой
перепроверить. Если задача не воспроизводится — скажи это и приведи, что
проверял, вместо починки наугад.

Stop: решения из §8 (за владельцем), любое действие из §9, и всё, что требует
живого прогона с реальными тратами.
