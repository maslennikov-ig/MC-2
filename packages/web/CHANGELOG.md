# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Fixed - 2025-01-24
- 🔒 **Исправлена проблема отображения видео из-за RLS политик**
  - **Проблема**: Видео не отображались на странице курса, хотя существовали в базе данных
  - **Причина**: RLS политики блокировали доступ к таблице `assets` для неопубликованных курсов
  - **Решение**: Использован admin клиент Supabase для загрузки ассетов, обходя RLS ограничения
  - **Файлы**: Изменен `/app/courses/[slug]/page.tsx`
  - **Результат**: Видео теперь корректно отображаются для всех курсов в режиме разработки

### Fixed - 2025-01-17
- 🚨 **Масштабное исправление всех багов в кодовой базе**
  - **Критические (P0) баги - ИСПРАВЛЕНЫ:**
    - Устранены все ошибки компиляции TypeScript (11 ошибок → 0)
    - Удалены ссылки на несуществующие таблицы БД (tests, questions, documents, document_chunks, sources)
    - Добавлен fallback для отсутствующей функции rate_limit с корректной обработкой ошибок
  - **Высокоприоритетные (P1) баги - ИСПРАВЛЕНЫ:**
    - Устранены утечки памяти в 5 компонентах (добавлены cleanup функции для setTimeout/setInterval)
    - Исправлены зависимости React хуков (добавлен router в useEffect dependencies)
    - Решена несовместимость с Edge Runtime (добавлен export runtime = 'nodejs')
  - **Средние (P2) баги - ИСПРАВЛЕНЫ:**
    - Заменено 37 использований типа 'any' на корректные TypeScript типы
    - Удалены/префиксированы 10 неиспользуемых переменных
    - Удален вывод пароля в консоль (security issue)
  - **Низкие (P3) баги - ИСПРАВЛЕНЫ:**
    - Удалены console.log из 20+ файлов продакшн кода
    - Исправлены все оставшиеся ESLint warnings (27 → 0)
  - **Результат:**
    - TypeScript: 0 ошибок компиляции ✅
    - ESLint: 0 ошибок, 0 предупреждений ✅
    - Готовность к деплою: 100% ✅

### Fixed
- 🎨 **Исправлена проблема с отображением Header в тёмной теме на страницах курсов**
  - **Проблема**: Header отображался в тёмном стиле на всех страницах, включая страницы курсов
  - **Решение**:
    - Компонент Header теперь принимает параметр `darkMode` (по умолчанию `false`)
    - Тёмный стиль применяется только на главной странице и странице создания курса
    - На остальных страницах Header адаптируется к текущей теме приложения
  - **Результат**: Правильное отображение Header согласно дизайну каждой страницы

- 🐛 **Исправлена ошибка Hydration при отображении времени в прогрессе генерации**
  - **Проблема**: Несовпадение форматирования времени между сервером и клиентом вызывало ошибку hydration
  - **Решение**: Заменена функция `toLocaleTimeString()` на кастомную `formatTime()` с единым форматом `HH:MM:SS`
  - **Результат**: Устранены ошибки hydration на странице генерации курса

- 🚀 **Исправлена критическая проблема с обновлением статуса генерации курса в реальном времени**
  - **Проблема**: Статус генерации курса не обновлялся на фронтенде, несмотря на успешные вызовы webhook из n8n
  - **Причина**:
    - Дублирующиеся RPC функции в Supabase с разными типами параметров (`jsonb` и `text` для `p_error_details`)
    - Неправильные ссылки на `course_id` в RPC нодах n8n workflow
  - **Решение**:
    - Удалена дублирующая RPC функция с параметром `p_error_details` типа `text`
    - Оставлена единственная версия функции с `p_error_details` типа `jsonb`
    - Исправлены все RPC ноды в Test-RPC workflow для использования корректной ссылки: `$('Get Course from DB').item.json.id`
  - **Результат**:
    - ✅ Статус генерации успешно обновляется в реальном времени
    - ✅ Все 5 шагов прогресса (20%, 40%, 60%, 80%, 100%) корректно отображаются
    - ✅ Протестировано на двух успешных генерациях курсов
    - ✅ n8n логи показывают успешное выполнение всех RPC вызовов
  - **Тестовый workflow**: Test-RPC (ID: RM3Kjcw6UbfawaFO)

### Fixed
- 🔧 **Исправлена критическая проблема с бесконечным циклом рендеринга в системе аутентификации**
  - **Проблема**: AuthButton показывал бесконечный Skeleton, профиль не загружался, сотни ре-рендеров в секунду
  - **Причина**: Неправильные зависимости в useEffect хуках создавали каскад обновлений
  - **Решение**:
    - Удален отладочный код, вызывавший лишние ре-рендеры (`components/common/auth-button.tsx`)
    - Исправлены зависимости useEffect: изменено с `[session, supabase]` на `[session?.user?.id]`
    - Добавлена защита от зависания с 5-секундным таймаутом для загрузки профиля
    - Исправлены зависимости в `lib/supabase/browser-client.tsx`: убран `session` из зависимостей интервала обновления токена
  - **Результат**:
    - ✅ Кнопка входа отображается корректно (не застревает в состоянии Skeleton)
    - ✅ Профиль загружается успешно
    - ✅ Меню динамически меняется в зависимости от статуса авторизации
    - ✅ Вход/выход работают без проблем
    - ✅ Устранены бесконечные ре-рендеры

### Changed
- 🔄 **Упрощена система отслеживания прогресса генерации курса с 6 до 5 шагов**
  - **Проблема**: RPC вызовы из n8n workflows не работали из-за неправильного формата тела запроса
  - **Причина**: n8n использовал `bodyParameters` вместо `specifyBody: "json"` с `jsonBody`
  - **Решение**:
    - Исправлен формат RPC нод для корректной работы с Supabase
    - Упрощена система с 6 шагов до 5, исключив опциональные этапы обработки документов и суммаризации
    - Обновлена RPC функция `update_course_progress` в Supabase для работы с 5 шагами
  - **Обновленные компоненты**:
    - `components/course/generation-progress.tsx` - UI теперь показывает 5 шагов
    - `app/actions/courses.ts` - инициализация создает 5 шагов вместо 6
    - `app/api/webhooks/coursegen/route.ts` - обработка webhook для 5 шагов
  - **Новые 5 шагов**:
    1. Инициализация (Rocket)
    2. Анализ задачи (Brain)
    3. Генерация структуры (BookOpen)
    4. Создание контента (Sparkles)
    5. Финализация (CheckCircle)
  - **Документация**: создан файл `workflows under work/rpc_nodes_5_steps_SIMPLE.md` с готовыми нодами для n8n
  - **Преимущества**:
    - Только 5 RPC нод вместо 7-8
    - Каждый новый workflow автоматически отмечает завершение предыдущего шага
    - Нет дублирования и переусложнения логики
    - Последовательное обновление статусов: 1→2→3→4→5

### Fixed
- 🐛 **Исправлена ошибка двойного пути в n8n webhook URL**
  - Проблема: При вызове webhook добавлялся `/generate` к URL, который уже содержал этот путь
  - Результат: n8n возвращал 404 ошибку "The requested webhook 'POST coursegen/generate/generate' is not registered"
  - Решение: Убрано добавление `/generate` в коде, используется полный URL из переменной окружения
  - Файл: `app/actions/courses.ts` строка 304
  - Статус: Webhook теперь доступен, но требуется настройка авторизации (403 "Authorization data is wrong!")
  - **Требуется действие**: n8n webhook требует Basic Auth, нужны правильные учетные данные для `flow8n.ru`
  - Добавлен код поддержки Basic Auth в `app/actions/courses.ts` (переменные N8N_WEBHOOK_AUTH_USER и N8N_WEBHOOK_AUTH_PASS)

### Fixed
- 🐛 **Исправлен конфликт динамических маршрутов, препятствующий запуску приложения**
  - Проблема: Next.js выдавал ошибку "You cannot use different slug names for the same dynamic path ('id' !== 'slug')"
  - Причина: одновременное существование папок `[id]` и `[slug]` в `app/api/courses/`
  - Решение: полностью удалена папка `[id]`, все API маршруты переведены на использование slug
  - Обновлен маршрут `cancel` для работы со slug вместо id
  - Теперь весь проект последовательно использует slug для идентификации курсов

### Changed
- 📝 **Упрощена валидация при создании курса**
  - Теперь обязательным является только название курса (topic)
  - Email автоматически берется из аутентифицированного пользователя
  - Все остальные поля стали опциональными с разумными значениями по умолчанию:
    - `difficulty`: по умолчанию 'beginner'
    - `style`: по умолчанию 'professional'
    - `output_formats`: по умолчанию ['text']
    - `content_strategy`: по умолчанию 'auto'
  - Обновлена серверная валидация в `app/actions/courses.ts`
  - Синхронизирована клиентская валидация в форме создания курса
  - Исправлена обработка опциональных полей при отправке FormData

### Improved
- ✨ **Улучшен UX процесса создания курса**
  - Реализована dedicated страница генерации (`/courses/[slug]/generating`) для отображения прогресса
  - После создания курса пользователь сразу попадает на страницу генерации вместо незавершенного курса
  - Страница генерации показывает пошаговый прогресс с real-time обновлениями через Supabase Realtime
  - Автоматический редирект на страницу курса после завершения генерации
  - Убран промежуточный компонент CourseGenerationProgress со страницы курса
  - Если пользователь попадает на страницу незавершенного курса, автоматически редиректится на страницу генерации
  - Этот подход является best practice для длительных операций (immediate feedback, progress visibility, better error handling)

### Fixed
- 🔐 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Решена проблема с серверной аутентификацией (SSR)
  - Проблема: сервер не мог прочитать состояние аутентификации, хотя клиент был залогинен
  - Причина: custom storage implementation использовала localStorage вместо cookies
  - Решение: удален custom storage object из browser-client.tsx
  - Теперь Supabase использует default cookie-based storage для совместимости с SSR
  - Cookies теперь имеют правильный формат: `sb-<project-ref>-auth-token`
  - Требуется очистка браузерных данных и повторный вход после обновления

### Fixed
- 🚨 **КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ**: Устранена проблема с постоянной потерей сессии аутентификации
  - Проблема: состояние аутентификации сбрасывалось при навигации между страницами, требуя Ctrl+F5 для восстановления
  - Решение: добавлена директива `force-dynamic` для отключения статической генерации страниц
  - Обновлены все страницы приложения для корректной работы с динамическим рендерингом
  - Создана серверная обертка для клиентских страниц (главная, создание курса)
  - Исправлен singleton паттерн Supabase client, теперь создается новый экземпляр для каждого запроса
  - Удалены агрессивные cache headers, мешавшие работе middleware
  - Обновлены компоненты Header и AuthButton для использования `getUser()` вместо `getSession()`
  - Добавлена правильная обработка событий `onAuthStateChange` с проверкой при фокусе окна
  - Настроен правильный PKCE flow для cookie-based сессий в SSR

### Changed
- 🎨 Хедер на главной странице и странице создания курса теперь всегда отображается в темных цветах
  - Добавлен проп darkMode в компонент AuthButton для управления стилями
  - Обновлены классы навигационных кнопок для постоянного темного вида
  - Применены темные стили независимо от выбранной глобальной темы

### Fixed
- 🔧 Исправлена проблема с карточками курсов, остающимися в темной теме при переключении
  - Заменены жестко закодированные темные цвета на адаптивные классы Tailwind
  - Добавлены правильные dark: префиксы для всех цветовых классов
  - Исправлены фоны, границы и цвета текста для корректной работы с темами

- 🔧 Исправлена проблема с отсутствующими надписями кнопок в хедере на десктопе
  - Изменены классы видимости с `hidden xs:inline` на `hidden sm:inline`
  - Восстановлены надписи "Каталог" и "Создать курс" в навигационном меню
- 🔧 Исправлена критическая проблема с потерей состояния аутентификации при навигации между страницами
  - Обновлен middleware для правильной работы с cookies согласно последним best practices Supabase SSR
  - Использование `getUser()` вместо `getSession()` для более надежной проверки аутентификации
  - Добавлены правильные cache headers для предотвращения кеширования устаревшего состояния

- 🔧 Исправлена проблема с автоматическим редиректом после входа
  - Убран принудительный редирект на `/courses` после входа
  - Пользователь теперь остается на текущей странице после успешной аутентификации
  - Сохранена поддержка опционального параметра `returnTo` для целевых редиректов

- 🔧 Исправлена проблема с кнопкой "Создать курс" требующей Ctrl+F5 для работы
  - Состояние кнопки теперь корректно обновляется после изменения статуса аутентификации

- 🐛 Исправлена ошибка React с передачей `elementRef` в DOM элемент
  - Проблема возникала в компоненте CourseViewerEnhanced при использовании свайпов
  - Деструктурированы только необходимые обработчики из хука useSwipe
  - Устранено предупреждение "React does not recognize the elementRef prop"

### Added
- ✨ Создан AuthSyncProvider для глобальной синхронизации состояния аутентификации
  - Автоматическое обновление токенов каждые 30 секунд
  - Синхронизация состояния при переключении между вкладками браузера
  - Обработка событий смены состояния: SIGNED_IN, SIGNED_OUT, TOKEN_REFRESHED
  - Проверка валидности сессии при возвращении фокуса на вкладку

### Changed
- 🎨 Изменена тема по умолчанию на светлую для улучшения UX
  - Светлая тема теперь используется по умолчанию на страницах курсов и чтения
  - Сохранена возможность переключения на темную тему через toggle
  - Главная страница и страница создания курса остаются с принудительной темной темой

- 📦 Обновлены клиентские компоненты для использования более надежных методов проверки аутентификации
  - hero-content.tsx: добавлена обработка видимости страницы и использование getUser()
  - auth-button.tsx: улучшена синхронизация с серверным состоянием
  - login-form.tsx: оптимизирована последовательность действий после входа

### Technical
- Обновлены все Supabase клиенты для соответствия паттерну @supabase/ssr
- Добавлена корректная обработка cookies с использованием getAll() и setAll()
- Внедрена система двусторонней синхронизации между клиентом и сервером
- Улучшена обработка ошибок и edge cases в процессе аутентификации

## [0.1.0] - 2024-01-11

### Added
- Initial release of CourseAI platform
- Automated course generation using AI
- Document processing and analysis
- Multi-format course content generation (video, audio, text)
- User authentication with Supabase
- Dark/Light theme support
- Responsive design