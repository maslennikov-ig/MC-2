# Техническое задание: Улучшение типизации Supabase в проекте CourseAI

## 📋 Текущая ситуация

### Проблемы
1. **Неправильный вывод типов Supabase** - операции `update` и `insert` возвращают тип `never`
2. **Временные решения** - используются `@ts-ignore` директивы с отключением ESLint правил
3. **Ручное обновление типов** - типы генерируются вручную через MCP или CLI
4. **Риск рассинхронизации** - типы могут устареть при изменениях схемы БД

### Текущие костыли
```typescript
// app/courses/actions.ts
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore - Supabase type inference issue with update
const { error } = await supabase
  .from('courses')
  .update({ is_published: !isPublished })
```

## 🎯 Цель

Настроить автоматическую генерацию и правильное использование типов Supabase для обеспечения полной type safety без использования директив подавления ошибок.

## 📝 Задачи

### 1. Настройка автоматической генерации типов

#### 1.1 Локальная генерация
- [ ] Установить Supabase CLI глобально
- [ ] Создать npm скрипт для генерации типов:
  ```json
  {
    "scripts": {
      "types:generate": "supabase gen types typescript --project-id $SUPABASE_PROJECT_ID > types/database.generated.ts",
      "types:check": "tsc --noEmit types/database.generated.ts"
    }
  }
  ```
- [ ] Добавить в `.env.local` переменную `SUPABASE_PROJECT_ID`
- [ ] Документировать процесс в README.md

#### 1.2 CI/CD автоматизация
- [ ] Создать GitHub Action `.github/workflows/generate-types.yml`:
  ```yaml
  name: Generate Supabase Types
  on:
    schedule:
      - cron: '0 0 * * *' # Ежедневно
    workflow_dispatch: # Ручной запуск
  ```
- [ ] Настроить автоматический коммит обновленных типов
- [ ] Добавить проверку на изменения перед коммитом
- [ ] Настроить уведомления при обновлении типов

### 2. Исправление проблем с выводом типов

#### 2.1 Правильная типизация клиентов Supabase
- [ ] Создать типизированные клиенты:
  ```typescript
  // lib/supabase/typed-client.ts
  import { createClient } from '@supabase/supabase-js'
  import { Database } from '@/types/database.generated'
  
  export const createTypedClient = () => {
    return createClient<Database>(url, key)
  }
  ```

#### 2.2 Создание хелперов для операций
- [ ] Создать типобезопасные обертки для проблемных операций:
  ```typescript
  // lib/supabase/helpers.ts
  import { Database } from '@/types/database.generated'
  
  type Tables = Database['public']['Tables']
  type CourseUpdate = Tables['courses']['Update']
  type UserFavoriteInsert = Tables['user_favorites']['Insert']
  
  export async function updateCourse(
    supabase: SupabaseClient<Database>,
    slug: string,
    data: CourseUpdate
  ) {
    return supabase
      .from('courses')
      .update(data)
      .eq('slug', slug)
  }
  ```

#### 2.3 Миграция существующего кода
- [ ] Заменить все прямые вызовы `supabase.from()` на типизированные хелперы
- [ ] Удалить все `@ts-ignore` и `@ts-expect-error` директивы
- [ ] Проверить все файлы с операциями БД:
  - `app/courses/actions.ts`
  - `app/api/courses/create/route.ts`
  - `app/api/courses/[slug]/route.ts`
  - `app/api/courses/paginated/route.ts`

### 3. Улучшение DX (Developer Experience)

#### 3.1 Автокомплит и IntelliSense
- [ ] Настроить VSCode для работы с сгенерированными типами
- [ ] Создать сниппеты для частых операций
- [ ] Добавить JSDoc комментарии к хелперам

#### 3.2 Валидация на этапе разработки
- [ ] Добавить pre-commit хук для проверки типов:
  ```bash
  # .husky/pre-commit
  pnpm type-check
  pnpm types:check
  ```
- [ ] Настроить ESLint правила для обязательной типизации Supabase операций

### 4. Мониторинг и поддержка

#### 4.1 Документация
- [ ] Создать `docs/supabase-types.md` с описанием:
  - Процесс генерации типов
  - Решение частых проблем
  - Примеры использования хелперов
  - FAQ по типизации

#### 4.2 Тестирование
- [ ] Написать unit тесты для хелперов
- [ ] Добавить интеграционные тесты для операций с БД
- [ ] Настроить тестовое окружение с моками Supabase

### 5. Долгосрочные улучшения

#### 5.1 Миграция на Prisma (опционально)
- [ ] Исследовать возможность использования Prisma с Supabase
- [ ] Сравнить производительность и DX
- [ ] Подготовить план миграции если необходимо

#### 5.2 Кастомный генератор типов
- [ ] Рассмотреть создание своего генератора на основе схемы БД
- [ ] Добавить поддержку кастомных типов для JSONB полей
- [ ] Генерировать Zod схемы вместе с TypeScript типами

## 📊 Критерии успеха

1. **Нулевое количество `@ts-ignore` директив** в коде
2. **100% type safety** для всех операций с БД
3. **Автоматическая генерация** типов при изменении схемы
4. **Отсутствие ошибок** типизации при сборке
5. **Улучшенный DX** с полным автокомплитом

## 🗓️ Временные оценки

| Этап | Оценка времени | Приоритет |
|------|---------------|-----------|
| Настройка автоматической генерации | 2-3 часа | Высокий |
| Исправление проблем с выводом типов | 4-6 часов | Критический |
| Улучшение DX | 2-3 часа | Средний |
| Мониторинг и поддержка | 3-4 часа | Средний |
| Долгосрочные улучшения | 1-2 недели | Низкий |

**Общая оценка:** 12-16 часов на базовые улучшения

## 🚀 План внедрения

### Фаза 1: Быстрые победы (1-2 дня)
1. Настроить локальную генерацию типов
2. Создать базовые хелперы для критичных операций
3. Исправить самые проблемные места в коде

### Фаза 2: Автоматизация (3-4 дня)
1. Настроить CI/CD для генерации типов
2. Мигрировать весь код на типизированные хелперы
3. Добавить проверки и валидацию

### Фаза 3: Полировка (1 неделя)
1. Документация и обучение команды
2. Тестирование и мониторинг
3. Оптимизация процессов

## 📚 Ресурсы

- [Supabase TypeScript Guide](https://supabase.com/docs/guides/api/rest/generating-types)
- [GitHub Actions для Supabase](https://github.com/marketplace/actions/supabase-cli)
- [TypeScript Strict Mode](https://www.typescriptlang.org/tsconfig#strict)
- [Prisma с Supabase](https://supabase.com/docs/guides/integrations/prisma)

## ⚠️ Риски и митигация

| Риск | Вероятность | Влияние | Митигация |
|------|------------|---------|-----------|
| Breaking changes в Supabase типах | Средняя | Высокое | Версионирование типов, тесты |
| Конфликты при автогенерации | Низкая | Среднее | Review PR перед мержем |
| Производительность типизации | Низкая | Низкое | Инкрементальная компиляция |
| Сложность для новых разработчиков | Средняя | Среднее | Документация, примеры |

## ✅ Чеклист готовности

- [ ] Все `@ts-ignore` удалены
- [ ] Типы генерируются автоматически
- [ ] CI/CD настроен и работает
- [ ] Документация написана
- [ ] Команда обучена новому подходу
- [ ] Тесты написаны и проходят
- [ ] Мониторинг настроен

---

**Автор:** Claude AI  
**Дата создания:** 2025-01-09  
**Статус:** В разработке  
**Версия:** 1.0.0