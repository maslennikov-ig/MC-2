// ============================================================================
// CONSTANTS
// ============================================================================

/** Estimated tokens for Delta Judge verification per patch */
export const DELTA_JUDGE_ESTIMATED_TOKENS = 250;

/** Hard cap on refinement tasks started in a single iteration. */
export const MAX_TASKS_PER_ITERATION = 5;
