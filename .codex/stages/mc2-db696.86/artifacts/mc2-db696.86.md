---
schema_version: orchestration-artifact/v1
artifact_type: delegated-stream
task_id: mc2-db696.86
stage_id: mc2-db696.86
agent_type: debugger
subagent_model: inherit_orchestrator
reasoning_effort: inherit_orchestrator
model_reasoning_rationale: Endpoint/runtime configuration diagnosis with no model override.
repo: /home/me/code/mc2
branch: codex/career-playbook-live-smoke-fixes
base_branch: codex/career-playbook-live-smoke-fixes
base_commit: ac7987de7828d6a853399db022f3ff9545f5b560
worktree: /home/me/code/mc2
write_zone:
  - docker-compose.dev.yml
  - .codex/stages/mc2-db696.86/artifacts/mc2-db696.86.md
success_criteria:
  - Determine whether Qdrant 404 is endpoint/config, auth, missing collection, client URL, or smoke runtime wiring.
  - Make dev runtime use intended Qdrant service without leaking secrets.
  - Avoid cloud Qdrant mutations during diagnosis.
selected_docs:
  - AGENTS.md
  - .codex/orchestrator.toml
  - .codex/handoff.md
  - graphify-out/GRAPH_REPORT.md
  - official Qdrant Cloud docs
selected_skills:
  - orchestrator-stage
  - systematic-debugging
  - prompt-authoring
selected_agents:
  - Volta debugger subagent
catalog_candidates:
  - none
parallel_group: S1
depends_on_streams:
  - none
parallel_decision: parallel
status: blocked
delivery_method: manual integration
accepted_by_orchestrator: yes
cleanup_status: cleaned
cleanup_notes: Volta subagent closed; no child worktree cleanup required.
risk_level: medium
docs_impact: ops-deploy
docs_reviewed: no-change-needed
docs_review_notes: Tracked dev-compose wiring fix; no durable operator command changed.
verification:
  - masked read-only Qdrant probe: current cloud endpoint returned plain 404 on /collections
  - docker compose -f docker-compose.dev.yml config --no-interpolate --quiet: passed
  - git diff --check: passed
changed_files:
  - docker-compose.dev.yml
  - .codex/stages/mc2-db696.86/artifacts/mc2-db696.86.md
explicit_defers:
  - Correct cloud Qdrant endpoint/API key remains external; current configured cloud endpoint returns route-level 404.
---

# Summary

Diagnosed the Qdrant follow-up and fixed the tracked dev runtime wiring.

The current cloud URL/API-key pair reaches a host that returns plain HTTP `404 page not found` for `/`, `/collections`, and `/collections/course_embeddings`. That means the failure is at endpoint/config level, not a missing collection: if only `course_embeddings` were absent, `/collections` would still succeed.

The tracked dev compose stack already starts `qdrant-dev` and deploy health checks `localhost:6333`, but `api-dev`, `worker-dev`, and `worker-stage6-dev` inherited `QDRANT_URL` from `.env.dev`. The compose file now overrides those services to `http://qdrant-dev:6333`, so dev containers use the intended local Qdrant service.

# Scope / Routing

Volta performed a read-only diagnosis. The orchestrator applied the compose wiring fix locally. No cloud mutation, collection creation, upsert, or delete was performed.

# Verification

- Masked read-only probe reproduced route-level 404 for the configured cloud endpoint.
- `docker compose -f docker-compose.dev.yml config --no-interpolate --quiet` passed.
- `git diff --check` passed.

# Delivery / Cleanup

Accepted and integrated locally. Volta was closed after completion.

# Risks / Follow-ups / Explicit Defers

The cloud endpoint/key itself remains an external blocker. A valid active Qdrant database REST endpoint and matching key are required before cloud-backed smoke can pass without the dev-compose local Qdrant path.
