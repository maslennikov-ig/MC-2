---
schema_version: orchestration-artifact/v1
artifact_type: delegated-stream
task_id: mc2-db696.14
stage_id: mc2-db696.14
agent_type: worker
subagent_model: inherit_orchestrator
reasoning_effort: inherited
model_reasoning_rationale: correctness review for backend PDF rendering, auth, packaging, tests, and production runtime risk
repo: /home/me/code/mc2
branch: feature/career-playbook-pdf
base_branch: feature/career-playbook-library-share
base_commit: 7ef1a88881e939238af846fcc8d586fec6c22488
worktree: /home/me/code/mc2/.worktrees/feature-career-playbook-pdf-review
write_zone:
  - read-only
success_criteria:
  - Identify blocking bugs, regressions, missing tests, security issues, and verification gaps.
  - Include severity, evidence, suggested fix, value, tradeoff, and confidence.
selected_docs:
  - Context7 /microsoft/playwright
  - Context7 /websites/trpc_io
selected_skills:
  - code-review
  - requesting-code-review
selected_agents:
  - visible Codex subagent Ramanujan
catalog_candidates:
  - code-review-and-quality lookup-only
parallel_group: review
depends_on_streams:
  - none
parallel_decision: parallel
status: accepted
delivery_method: manual integration
accepted_by_orchestrator: yes
cleanup_status: cleaned
cleanup_notes: read-only subagent closed; no child worktree or branch cleanup required
risk_level: high
verification:
  - reviewer report returned
  - orchestrator accepted Docker runtime finding
  - orchestrator rejected base64 streaming change as non-blocking MVP improvement
changed_files:
  - none
explicit_defers:
  - Base64 JSON streaming alternative remains deferred until real payload size requires it.
---

# Summary

Ramanujan reviewed PR #34 for correctness and found one blocking production bug: the new PDF export launches Playwright Chromium, but the production API image did not install browser binaries or system dependencies.

# Findings

## Must-Fix

| Severity | Finding                                                  | Evidence                                                                                                                                                                  | Suggested Fix                                                                                                                      | Expected Value                           | Tradeoff                       | Confidence | Orchestrator Decision                   |
| -------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ------------------------------ | ---------- | --------------------------------------- |
| High     | Production API container cannot run PDF export reliably. | `career-playbook-pdf.ts` imports and launches `chromium`; `packages/course-gen-platform/Dockerfile` used Alpine runner and installed only `dumb-init`, `curl`, and `tsx`. | Use a Playwright-compatible Linux runtime, install Chromium and system dependencies during image build, and add a container smoke. | Prevents production-only export failure. | Larger image and slower build. | High       | Accepted as `mc2-db696.14.2` and fixed. |

## Optional Improvements

| Severity | Finding                                                    | Evidence                                                       | Suggested Fix                                                   | Expected Value                                    | Tradeoff                    | Confidence | Orchestrator Decision                            |
| -------- | ---------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------- | --------------------------- | ---------- | ------------------------------------------------ |
| Medium   | Base64 PDF JSON response is less efficient than streaming. | `exportPdf` returns `pdfBase64` from a tRPC query.             | Consider authenticated streaming route or signed URL after MVP. | Lower memory and payload overhead for large PDFs. | More route/client plumbing. | Medium     | Rejected for this pass; tracked as MVP tradeoff. |
| Low      | Static PDF shell text was English for Russian playbooks.   | Template hardcoded `Table of contents` and English cover note. | Add small `ru`/`en` chrome dictionary.                          | More polished bilingual output.                   | Minor copy maintenance.     | High       | Accepted and fixed locally.                      |

# Verification

The orchestrator reproduced and fixed the production runtime issue with a successful Docker runner build and an in-container Chromium launch smoke. The localization improvement is covered by a new unit test.

# Risks / Follow-ups

Base64 response shape is acceptable for MVP but should be revisited if observed PDF sizes become large. No remaining correctness blocker from this report is open.
