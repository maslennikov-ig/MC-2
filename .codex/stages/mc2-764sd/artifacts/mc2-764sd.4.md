---
task_id: mc2-764sd.4
stage_id: mc2-764sd
repo: mc2
branch: codex/mc2-764sd-dev-qdrant-endpoint-fix
base_branch: develop
base_commit: 991a9c4d5f668a072332e51036cac9a5f95bceb7
worktree: /home/me/.config/superpowers/worktrees/mc2/codex/mc2-764sd-dev-qdrant-endpoint-fix
status: blocked
verification:
  - Context7 `/qdrant/qdrant-js` client docs lookup: passed
  - ssh megacampus-prod "docker exec megacampus-worker-dev node --input-type=module -e ... client.getCollections() ...": failed
  - gh secret list -R maslennikov-ig/mc-2: passed
  - git diff --check: passed
  - git status --short: passed
changed_files:
  - .codex/stages/mc2-764sd/artifacts/mc2-764sd.4.md
---

# Summary

Repair could not be completed safely because the **correct live Dev Qdrant endpoint is not discoverable from the currently accessible runtime/ops sources**.

Confirmed findings:

- Dev containers and `/opt/megacampus/.env.dev` use `QDRANT_URL=https://b66349de-ad5f-4d43-aa6e-8a2aab53542a.eu-central-1-0.aws.cloud.qdrant.io:6333`
- From inside `megacampus-worker-dev`, the official `@qdrant/js-client-rest` client still returns:
  - `message: "Not Found"`
  - `actual.status: 404`
  - `actual.data: "404 page not found\n"`
- Direct HTTP to the configured endpoint also returns `404 page not found` for `/collections`
- Dev deploy source of truth is **GitHub Actions secret** `QDRANT_URL` (`.github/workflows/ci-cd.yml:790-791`), not a committed file
- `gh secret list -R maslennikov-ig/mc-2` confirms the secret exists, but current permissions do not allow reading its value

Because the task explicitly forbids inventing a replacement endpoint, and no second live endpoint was present in:

- server env files / backups
- running dev/staging containers
- repo docs / git history / local artifacts
- accessible server filesystem

I did **not** mutate Dev env files or restart services. Doing so would have been guesswork.

# Verification

## Context7 / client contract

Used Context7 for `/qdrant/qdrant-js` and verified:

- cloud client is initialized with a configured base URL plus `apiKey`
- `getCollections()` / `getCollection()` hit that base URL directly
- non-OK HTTP responses surface as typed client errors

This matches the runtime reproduction below.

## Exact commands run

```bash
ssh -G megacampus-prod | sed -n '1,120p'
ssh megacampus-prod "hostname && pwd && docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Image}}' | grep -E 'megacampus-(api|worker|web|notebooklm).*dev|NAMES'"
ssh megacampus-prod 'for c in megacampus-worker-dev megacampus-api-dev megacampus-worker megacampus-web-dev; do if docker inspect "$c" >/dev/null 2>&1; then echo "=== $c ==="; docker inspect "$c" --format "{{range .Config.Env}}{{println .}}{{end}}" | grep -E "^(QDRANT_URL|QDRANT_API_KEY)=" | sed -E "s#(QDRANT_API_KEY=).+#\1***MASKED***#"; fi; done'
ssh megacampus-prod 'cd /opt/megacampus && sed -n "/^QDRANT_URL=/p;/^QDRANT_API_KEY=/p" .env.backup .env.dev .env.blue .env.green .env.production | sed -E "s#(QDRANT_API_KEY=).+#\1***MASKED***#"'
ssh megacampus-prod 'base="https://b66349de-ad5f-4d43-aa6e-8a2aab53542a.eu-central-1-0.aws.cloud.qdrant.io"; key=$(sed -n "s/^QDRANT_API_KEY=//p" /opt/megacampus/.env.dev); for u in "$base" "$base:6333"; do echo "=== $u ==="; curl -sS -o /tmp/q.out -D /tmp/q.headers -H "api-key: $key" "$u/collections"; sed -n "1,12p" /tmp/q.headers; echo BODY:; sed -n "1,5p" /tmp/q.out; echo; done'
ssh megacampus-prod 'python3 - <<\"PY\"
import json, base64
from pathlib import Path
key = next(line.split("=",1)[1].strip() for line in Path("/opt/megacampus/.env.dev").read_text().splitlines() if line.startswith("QDRANT_API_KEY="))
parts = key.split(".")
print(json.dumps({"length": len(key), "segments": len(parts), "prefix": key[:5]}, indent=2))
if len(parts) >= 2:
    for idx, part in enumerate(parts[:2]):
        pad = chr(61) * (-len(part) % 4)
        raw = base64.urlsafe_b64decode(part + pad)
        try:
            obj = json.loads(raw)
        except Exception:
            obj = raw.decode("utf-8", "replace")
        print(f"SEGMENT_{idx}=")
        print(json.dumps(obj, indent=2, ensure_ascii=False))
PY'
ssh megacampus-prod 'docker exec megacampus-worker-dev node --input-type=module -e "import { QdrantClient } from \"@qdrant/js-client-rest\"; const client = new QdrantClient({ url: process.env.QDRANT_URL, apiKey: process.env.QDRANT_API_KEY }); try { const r = await client.getCollections(); console.log(JSON.stringify({ ok: true, collections: r.collections.map(c => c.name) }, null, 2)); } catch (e) { const actual = typeof e?.getActualType === \"function\" ? e.getActualType() : null; console.log(JSON.stringify({ ok: false, message: e?.message, actual }, null, 2)); process.exit(1); }"'
ssh megacampus-prod 'ss -tulpn | grep -E ":6333|:6334" || true'
ssh megacampus-prod 'systemctl list-units --type=service --all | grep -i qdrant || true'
ssh megacampus-prod 'ps aux | grep -i [q]drant || true'
gh auth status
gh secret list -R maslennikov-ig/mc-2
nl -ba .github/workflows/ci-cd.yml | sed -n '620,650p'
nl -ba .github/workflows/ci-cd.yml | sed -n '780,805p'
git diff --check
git status --short
```

## Fresh evidence

### Live Dev failure mode still present

Inside the real Dev worker:

```json
{
  "ok": false,
  "message": "Not Found",
  "actual": {
    "status": 404,
    "data": "404 page not found\n"
  }
}
```

Direct HTTP against the configured host also returns:

```text
HTTP/2 404
content-type: text/plain; charset=utf-8

404 page not found
```

### Why safe remediation is blocked

- `.github/workflows/ci-cd.yml:790-791` writes Dev `QDRANT_URL` / `QDRANT_API_KEY` from GitHub secrets into `/opt/megacampus/.env.dev`
- `gh secret list -R maslennikov-ig/mc-2` confirms `QDRANT_URL` exists as a secret
- GitHub does not expose repository secret values via `gh secret list`, and no alternative accessible source revealed the live endpoint

### Collection health validation

Blocked. `course_embeddings` could not be validated because the base endpoint itself is still returning generic 404 before collection lookup starts.

### Restart scope

No services were restarted. There was no verified env correction to reload.

# Risks / Follow-ups / Explicit Defers

- Required follow-up: obtain the **actual live Dev `QDRANT_URL`** from a source that can reveal secret values (GitHub repo settings / secure vault / Qdrant Cloud dashboard).
- After that single value is available, the remaining repair path is straightforward:
  - update `/opt/megacampus/.env.dev`
  - restart only `api-dev` and Qdrant-consuming dev workers
  - verify from inside `megacampus-worker-dev`:
    - `client.getCollections()`
    - `client.getCollection('course_embeddings')` or create it via existing repo flow if missing
- No repo code changed. Only this tracked artifact was added in the dedicated task worktree.
