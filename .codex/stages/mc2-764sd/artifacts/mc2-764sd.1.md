---
task_id: mc2-764sd.1
stage_id: mc2-764sd
repo: mc2
branch: codex/mc2-764sd-stage6-ladder-fix
base_branch: develop
base_commit: 991a9c4d5f668a072332e51036cac9a5f95bceb7
worktree: /home/me/.config/superpowers/worktrees/mc2/codex/mc2-764sd-stage6-ladder-fix
status: returned
verification:
  - pnpm -F @megacampus/shared-types build: passed
  - pnpm -F @megacampus/course-gen-platform exec vitest run --config vitest.config.unit.ts tests/unit/stages/stage6-lesson-content/services/job-processor.test.ts: passed
  - pnpm -F @megacampus/course-gen-platform exec tsc --noEmit: blocked
  - git diff --check: passed
changed_files:
  - packages/course-gen-platform/src/stages/stage6-lesson-content/services/job-processor.ts
  - packages/course-gen-platform/tests/unit/stages/stage6-lesson-content/services/job-processor.test.ts
---

# Summary

Used Docs L1/L2 for current BullMQ retry semantics before implementation. The relevant contract is unchanged: ordinary failures stay retryable unless the processor intentionally short-circuits them as unrecoverable. Stage 6 was incorrectly doing that with a broad `mismatch` substring check.

Implemented a narrow Stage 6 fix in `packages/course-gen-platform/src/stages/stage6-lesson-content/services/job-processor.ts`:

- replaced broad `mismatch` short-circuit logic with message classification that distinguishes:
  - business-level quality exhaustion for `Max regeneration retries ... Review LessonSpecification ... mismatch`
  - true structural/input failures such as schema validation / invalid input mismatch families
- when the known LessonSpecification mismatch family appears, `processWithFallback()` now converts it into a `review_required`-style rung result so the outer automatic ladder records `quality_retryable` and continues promotion
- preserved current provider/runtime fallback behavior inside a rung for ordinary retryable failures
- preserved non-retryable handling for true schema/input mismatch errors

Moved the regression coverage into the normal tracked unit suite at `packages/course-gen-platform/tests/unit/stages/stage6-lesson-content/services/job-processor.test.ts`:

- exact `LessonSpecification ... mismatch` family is treated as quality-reviewable rather than a hard failure
- true schema mismatch (`Schema validation failed: sections mismatch`) remains non-retryable
- the outer ladder continues across `stage_6_simple -> stage_6_simple -> stage_6_normal -> stage_6_complex -> stage_6_auto_last_chance` instead of short-circuiting on the first rung

# Verification

- `pnpm -F @megacampus/shared-types build`
  - Passed under Node 20 via `npx -y node@20 ... pnpm.cjs` after linking the worktree package to the already-installed local `node_modules` so the required workspace dependency graph was available in the dedicated worktree.
- `pnpm -F @megacampus/course-gen-platform exec vitest run --config vitest.config.unit.ts tests/unit/stages/stage6-lesson-content/services/job-processor.test.ts`
  - Passed under Node 20.
  - This is the normal tracked Stage 6 unit suite path and repo unit config; no `/tmp` config or non-standard test discovery path was used in the final verification.
- `pnpm -F @megacampus/course-gen-platform exec tsc --noEmit`
  - Blocked by pre-existing repository issues outside this task's write zone.
  - Evidence after `shared-types` was built: unresolved `TS6305` outputs for `packages/shared-utils` / `packages/shared-logger` plus unrelated existing errors such as `src/server/routers/enrichment/procedures/create-batch.ts(242)` and `src/shared/llm/model-config-service.ts(767)`.
  - No `tsc` errors from the touched Stage 6 service or the updated tracked Stage 6 unit test were present in the captured output.
- `git diff --check`
  - Passed.

# Risks / Follow-ups / Explicit Defers

- Full `tsc --noEmit` for `@megacampus/course-gen-platform` is still blocked by unrelated monorepo baseline issues outside the Stage 6 write zone, chiefly missing built outputs for `shared-utils` / `shared-logger` in this dedicated worktree and pre-existing type errors in non-Stage-6 files.
