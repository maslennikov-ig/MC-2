---
schema_version: orchestration-artifact/v1
task_id: mc2-db696.13.3
stage_id: mc2-db696.13
repo: /home/me/code/mc2
branch: codex/career-playbook-generation-status
base_branch: feature/career-playbook-pdf
base_commit: de8efa066ad4d72bde2b5e0ee7e32bba31e45a59
worktree: /home/me/code/mc2/.worktrees/feature-career-playbook-pdf-review
status: accepted
delivery_method: manual integration
accepted_by_orchestrator: yes
cleanup_status: cleaned
cleanup_notes: read-only visible explorer review; no separate workspace cleanup required
risk_level: medium
verification:
  - read-only review report accepted by orchestrator
changed_files:
  - none - read-only review
explicit_defers:
  - none
---

# Summary

Reviewer: Wegener

Status: accepted for orchestration review.

## Must-fix findings accepted by orchestrator

| Severity | Finding                                                    | Evidence                                                                                                                | Suggested fix                                                                     | Expected value                                 | Tradeoff                                            | Confidence |
| -------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------------- | ---------- |
| High     | Career Playbook generation needed explicit worker routing. | `packages/course-gen-platform/src/orchestrator/processor.ts` did not route `JobType.CAREER_PLAYBOOK` before this stage. | Register `careerPlaybookHandler` in the processor.                                | Queue jobs are actually processed.             | Adds one handler route.                             | High       |
| High     | Career Playbook jobs must not fake a course relation.      | `job_status.course_id` is course-scoped, while playbook ids belong to `career_playbooks`.                               | Add a Career Playbook queue payload that carries `playbookId` without `courseId`. | Avoids corrupt cross-domain identifiers.       | Requires generic code to tolerate non-course jobs.  | High       |
| High     | Worker success/failure must persist terminal state.        | Frontend status reads `career_playbooks`, not BullMQ return values.                                                     | Persist generated blocks/final markdown/status/errors from the handler.           | User can observe completion and retry failure. | Requires DB writes in the worker path.              | High       |
| Medium   | Approve/enqueue needs idempotency.                         | Repeated clicks or tabs could enqueue duplicate generation work.                                                        | Use stable job id and return existing terminal/in-flight status.                  | Prevents duplicate expensive generation.       | Requires retry handling for retained terminal jobs. | High       |
| Medium   | Status/block access must be private by default.            | Role Guide output is user-provided role data.                                                                           | Restrict to owner or superadmin unless product later requires org sharing.        | Reduces data exposure.                         | Same-org sharing needs explicit future scope.       | Medium     |

## Optional findings accepted

- Keep frontend polling lifecycle-managed in React/page code and keep the store action one-shot.
- Treat transient polling errors separately from terminal backend `failed`.

# Verification

- Report reviewed by orchestrator and converted into backend/frontend acceptance checks.
- Follow-up code tests added for processor routing, queue payload, terminal persistence, idempotency, privacy, and polling lifecycle.

# Risks / Follow-ups

- No live worker/Redis/Supabase E2E was run in this pass; behavior is covered by unit tests and build gates.
