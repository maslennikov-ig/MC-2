---
schema_version: orchestration-artifact/v1
task_id: mc2-db696.13.3
stage_id: mc2-db696.13
repo: /home/me/code/mc2
branch: codex/career-playbook-generation-status
base_branch: feature/career-playbook-pdf
base_commit: de8efa066ad4d72bde2b5e0ee7e32bba31e45a59
worktree: /home/me/code/mc2/.worktrees/feature-career-playbook-pdf-review
status: accepted
delivery_method: manual integration
accepted_by_orchestrator: yes
cleanup_status: cleaned
cleanup_notes: visible review agents were read-only; no separate workspace cleanup required
risk_level: medium
verification:
  - correctness review by Goodall reviewed by orchestrator
  - improvement review by Bacon reviewed by orchestrator
  - accepted findings converted to mc2-db696.13.4
changed_files:
  - none - read-only review
explicit_defers:
  - hide percent progress while generating
  - completed-state CTA after generation
---

# Summary

Two visible review streams were run after the initial GREEN pass.

## Correctness Reviewer - Goodall

| Severity | Finding                                                                                                 | Evidence                                                                                                                               | Suggested fix                                                                             | Expected value                                   | Tradeoff                                                                                           | Confidence | Orchestrator decision                                             |
| -------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------- |
| High     | Retry after failed can stick because the stable `jobId` may collide with retained terminal BullMQ jobs. | `approveCareerPlaybookGeneration` uses `jobId: career-playbook:<id>` and `DEFAULT_JOB_OPTIONS[CAREER_PLAYBOOK].removeOnFail` is false. | Remove terminal completed/failed job before re-enqueue.                                   | Failed playbooks can be retried.                 | Extra queue lookup before enqueue.                                                                 | High       | Accepted and fixed in `removeTerminalJobById` plus service tests. |
| High     | Enqueue failure can leave DB status `generating` with no runnable job.                                  | Status was updated before `addJob`.                                                                                                    | Compensate the playbook row to `failed` and expose an error if enqueue fails.             | Prevents stuck UI.                               | One extra DB update on enqueue failure.                                                            | High       | Accepted and fixed.                                               |
| Medium   | Successful retry can retain stale `q_a_data.generation_error`.                                          | Completion path updates status and generated output but old `generation_error` could remain.                                           | Clear stored generation error on start/success and hide errors unless status is `failed`. | Completed playbooks do not show stale failure.   | Requires loading stored QA data on completion.                                                     | High       | Accepted and fixed.                                               |
| Medium   | Career Playbook jobs were not part of the generic `JobData` union.                                      | Generic queue/worker types expected `JobData`; Career Playbook schema was separate.                                                    | Add Career Playbook operation job data to `JobData`/`JobDataSchema`.                      | Avoids hidden casts and runtime validation gaps. | `JobDataSchema` becomes a plain union because Career Playbook uses nested operation discriminator. | High       | Accepted and fixed.                                               |
| Medium   | Editing while generating can desync persisted QA from the worker snapshot.                              | UI allowed edit navigation and backend `submitAnswer` accepted writes.                                                                 | Reject backend edits and disable/no-op UI edit during generation.                         | Worker snapshot remains coherent.                | Users must wait or retry after failure.                                                            | High       | Accepted and fixed.                                               |

## Improvement Reviewer - Bacon

| Severity | Finding                                                                 | Evidence                                                               | Suggested fix                                                  | Expected value                                                   | Tradeoff                                      | Confidence | Orchestrator decision |
| -------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------------------- | --------------------------------------------- | ---------- | --------------------- |
| High     | Handler returned failure instead of throwing, bypassing BullMQ retries. | `generatePlaybook` transformed graph errors into `{ success: false }`. | Throw retryable errors; persist failed only on final attempt.  | BullMQ retries work as configured.                               | Handler needs attempt-aware logic.            | High       | Accepted and fixed.   |
| High     | API could leave `generating` without runnable queue job.                | Same status-before-enqueue path.                                       | Add enqueue compensation and stale terminal job removal.       | Prevents stuck generation.                                       | Extra queue/DB work.                          | High       | Accepted and fixed.   |
| Medium   | Course cleanup could treat non-course Career Playbook jobs as orphaned. | Queue cleanup removed jobs without course ids.                         | Classify Career Playbook jobs as non-course jobs.              | Course deletion cleanup will not delete unrelated playbook work. | Adds a helper branch.                         | High       | Accepted and fixed.   |
| Low      | Persisting final markdown in localStorage is unnecessary and risky.     | Store `partialize` included generation output.                         | Keep final markdown in memory; do not persist to localStorage. | Reduces browser storage/privacy exposure.                        | Reload must refetch final output from server. | Medium     | Accepted and fixed.   |
| Low      | Fake progress percentages may be misleading.                            | UI displays coarse progress from status mapping.                       | Hide percent or replace with indeterminate status.             | More honest UX.                                                  | Removes existing visible progress signal.     | Medium     | Deferred.             |
| Low      | Add a completion next action.                                           | Completion UI only states success.                                     | Add library/viewer CTA.                                        | Better post-generation flow.                                     | Belongs to library/viewer scope.              | Medium     | Deferred.             |

# Verification

- Accepted findings were tracked in `mc2-db696.13.4`.
- Fixes were verified by focused backend/web/shared tests, `pnpm type-check`, `pnpm lint`, and `pnpm build`.

# Risks / Follow-ups

- Deferred UI refinements are tracked as explicit defers in this artifact rather than mixed into the generation transport fix.
