# MegaCampus Infrastructure Docker Compose
# Shared infrastructure services (run ONCE, shared by blue/green apps)
# Usage: docker compose -f docker-compose.infra.yml up -d

services:
  # ============================================
  # INFRASTRUCTURE SERVICES
  # ============================================

  # Redis for BullMQ queues and caching
  redis:
    image: redis:7-alpine
    container_name: megacampus-redis
    restart: unless-stopped
    ports:
      - '127.0.0.1:6379:6379'
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes --maxmemory 1gb --maxmemory-policy noeviction
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - megacampus
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  # Private self-hosted vector database for staging RAG consumers.
  qdrant:
    image: qdrant/qdrant:v1.18.2@sha256:75eab8c4ba42096724fdcfde8b4de0b5713d529dde32f285a1f86fdcb2c9e50c
    platform: linux/amd64
    container_name: megacampus-qdrant
    restart: unless-stopped
    entrypoint: ['/opt/megacampus/qdrant-secret-entrypoint.sh']
    ports:
      - '127.0.0.1:6335:6333'
    environment:
      - QDRANT_API_KEY_FILE=/run/secrets/qdrant_api_key
      - QDRANT_READ_ONLY_API_KEY_FILE=/run/secrets/qdrant_read_only_api_key
      - QDRANT_SNAPSHOT_STORAGE=local
      - QDRANT__STORAGE__SNAPSHOTS_PATH=/qdrant/storage/snapshots
      - QDRANT__TELEMETRY_DISABLED=true
      - QDRANT__SERVICE__METRICS_PREFIX=qdrant_
    volumes:
      - qdrant-data:/qdrant/storage
      - ./deploy/qdrant/secret-entrypoint.sh:/opt/megacampus/qdrant-secret-entrypoint.sh:ro
    secrets:
      - qdrant_api_key
      - qdrant_read_only_api_key
    healthcheck:
      test: ['CMD', '/opt/megacampus/qdrant-secret-entrypoint.sh', 'healthcheck']
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
    networks:
      - megacampus
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '3'

  # On-demand release-SHA operator for bootstrap/reindex and its isolated worker.
  # The profile prevents a normal infrastructure `up` from starting this service.
  qdrant-operator:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '0:0'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_COLLECTION_NAME=course_embeddings
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=/run/secrets/qdrant_api_key
      - DOCLING_UPLOADS_BASE_PATH=/opt/megacampus/data
      - BULLMQ_QUEUE_NAME=qdrant-reindex-disabled
      - QDRANT_REINDEX_TARGET_COLLECTION=
      - WORKER_CONCURRENCY=2
      - STAGE6_WORKER=false
      - REDIS_URL=redis://redis:6379
      - DOCLING_MCP_URL=http://docling-mcp:8000/mcp
      - DOCLING_CACHE_PATH=/app/docling-cache
      - LKG_CONFIG_PATH=/app/data/lkg-config.json
      - QDRANT_RECOVERY_STATE_DIR=/var/lib/megacampus-qdrant-recovery
      - QDRANT_METRICS_TEXTFILE_DIR=/var/lib/megacampus/qdrant-metrics
      - QDRANT_RECOVERY_LOCK_PATH=/var/lib/megacampus-qdrant-recovery/recovery.lock
      - QDRANT_RECOVERY_LOCK_HELD=0
      - QDRANT_METRICS_GID=${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}
    group_add:
      - '${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}'
    volumes:
      - ./data/uploads:/opt/megacampus/data/uploads:ro
      - docling-json-cache:/app/docling-cache
      - worker-data:/app/data
      - /var/lib/megacampus-qdrant-recovery:/var/lib/megacampus-qdrant-recovery
      - ${QDRANT_METRICS_TEXTFILE_HOST_DIR:?QDRANT_METRICS_TEXTFILE_HOST_DIR must be set}:/var/lib/megacampus/qdrant-metrics
    secrets:
      - qdrant_api_key
    tmpfs:
      - /tmp:size=64m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700
    networks:
      - megacampus

  # Narrow recovery service: no broad production env is loaded.
  qdrant-recovery-operator:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '0:0'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    environment:
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_COLLECTION_NAME=course_embeddings
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=/run/secrets/qdrant_api_key
      - QDRANT_SNAPSHOT_STORAGE_MODE=${QDRANT_SNAPSHOT_STORAGE_MODE:?QDRANT_SNAPSHOT_STORAGE_MODE must be local or s3}
      - QDRANT_SNAPSHOT_OBJECT_PREFIX=${QDRANT_SNAPSHOT_OBJECT_PREFIX:-}
      - QDRANT_RECOVERY_STATE_DIR=/var/lib/megacampus-qdrant-recovery
      - QDRANT_METRICS_TEXTFILE_DIR=/var/lib/megacampus/qdrant-metrics
      - QDRANT_RECOVERY_LOCK_PATH=/var/lib/megacampus-qdrant-recovery/recovery.lock
      - QDRANT_RECOVERY_LOCK_HELD=0
      - QDRANT_METRICS_GID=${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}
    group_add:
      - '${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}'
    volumes:
      - /var/lib/megacampus-qdrant-recovery:/var/lib/megacampus-qdrant-recovery
      - ${QDRANT_METRICS_TEXTFILE_HOST_DIR:?QDRANT_METRICS_TEXTFILE_HOST_DIR must be set}:/var/lib/megacampus/qdrant-metrics
    secrets:
      - qdrant_api_key
    tmpfs:
      - /tmp:size=32m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700
    networks:
      - megacampus

  # Restore adds only the owner-only manifest and deterministic recovery probe.
  qdrant-restore-operator:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '0:0'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    environment:
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_COLLECTION_NAME=course_embeddings
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=/run/secrets/qdrant_api_key
      - QDRANT_SNAPSHOT_TRANSPORT_URL=http://qdrant:6333
      - QDRANT_SNAPSHOT_MANIFEST_FILE=/run/secrets/snapshot_manifest
      - QDRANT_RECOVERY_PROBE_FILE=/run/secrets/recovery_probe
      - QDRANT_RECOVERY_STATE_DIR=/var/lib/megacampus-qdrant-recovery
      - QDRANT_METRICS_TEXTFILE_DIR=/var/lib/megacampus/qdrant-metrics
      - QDRANT_RECOVERY_LOCK_PATH=/var/lib/megacampus-qdrant-recovery/recovery.lock
      - QDRANT_RECOVERY_LOCK_HELD=0
      - QDRANT_METRICS_GID=${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}
    group_add:
      - '${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}'
    volumes:
      - /var/lib/megacampus-qdrant-recovery:/var/lib/megacampus-qdrant-recovery
      - ${QDRANT_METRICS_TEXTFILE_HOST_DIR:?QDRANT_METRICS_TEXTFILE_HOST_DIR must be set}:/var/lib/megacampus/qdrant-metrics
    secrets:
      - qdrant_api_key
      - source: qdrant_snapshot_manifest
        target: snapshot_manifest
      - source: qdrant_recovery_probe
        target: recovery_probe
    tmpfs:
      - /tmp:size=32m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700
    networks:
      - megacampus

  # Source-recovery planning and copy verification require database access but
  # receive both upload roots read-only. The only writable filesystem
  # capability outside reviewed state is the narrow same-filesystem probe bind.
  qdrant-source-recovery-planner:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '1001:1001'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=
      - QDRANT_READ_ONLY_API_KEY=
      - QDRANT_READ_ONLY_API_KEY_FILE=
      - SOURCE_RECOVERY_MANIFEST_PATH=/var/lib/megacampus-source-recovery/manifest.json
      - SOURCE_RECOVERY_JOURNAL_PATH=/var/lib/megacampus-source-recovery/progress/journal.json
      - SOURCE_RECOVERY_PLAN_INPUT_PATH=/run/source-recovery/plan-input.json
      - SOURCE_RECOVERY_CAPABILITY_PROBE_DIRECTORY=/run/source-recovery/capability
    volumes:
      - ${SOURCE_RECOVERY_DEVELOPMENT_UPLOAD_ROOT:?SOURCE_RECOVERY_DEVELOPMENT_UPLOAD_ROOT must be set}:/opt/megacampus/data/uploads-dev:ro
      - ${SOURCE_RECOVERY_PRODUCTION_UPLOAD_ROOT:?SOURCE_RECOVERY_PRODUCTION_UPLOAD_ROOT must be set}:/opt/megacampus/data/uploads:ro
      - ${SOURCE_RECOVERY_STATE_HOST_DIR:?SOURCE_RECOVERY_STATE_HOST_DIR must be set}:/var/lib/megacampus-source-recovery:rw
      - ${SOURCE_RECOVERY_PLAN_INPUT_FILE:?SOURCE_RECOVERY_PLAN_INPUT_FILE must be set}:/run/source-recovery/plan-input.json:ro
      - ${SOURCE_RECOVERY_CAPABILITY_HOST_DIR:?SOURCE_RECOVERY_CAPABILITY_HOST_DIR must be set}:/run/source-recovery/capability:rw
    tmpfs:
      - /tmp:size=32m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700,uid=1001,gid=1001
    networks:
      - megacampus
    command: ['source-recovery', '--help']

  # The copy executor has no network, environment file, or secrets. It can
  # read development uploads and the immutable reviewed manifest, and can
  # write only production uploads plus the owner-only progress journal.
  qdrant-source-recovery-executor:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '1001:1001'
    read_only: true
    network_mode: none
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    environment:
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=
      - QDRANT_READ_ONLY_API_KEY=
      - QDRANT_READ_ONLY_API_KEY_FILE=
      - SOURCE_RECOVERY_MANIFEST_PATH=/run/source-recovery/manifest.json
      - SOURCE_RECOVERY_JOURNAL_PATH=/run/source-recovery/progress/journal.json
    volumes:
      - ${SOURCE_RECOVERY_DEVELOPMENT_UPLOAD_ROOT:?SOURCE_RECOVERY_DEVELOPMENT_UPLOAD_ROOT must be set}:/opt/megacampus/data/uploads-dev:ro
      - ${SOURCE_RECOVERY_PRODUCTION_UPLOAD_ROOT:?SOURCE_RECOVERY_PRODUCTION_UPLOAD_ROOT must be set}:/opt/megacampus/data/uploads:rw
      - ${SOURCE_RECOVERY_MANIFEST_FILE:?SOURCE_RECOVERY_MANIFEST_FILE must be set}:/run/source-recovery/manifest.json:ro
      - ${SOURCE_RECOVERY_PROGRESS_HOST_DIR:?SOURCE_RECOVERY_PROGRESS_HOST_DIR must be set}:/run/source-recovery/progress:rw
    tmpfs:
      - /tmp:size=32m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700,uid=1001,gid=1001
    command: ['source-recovery', '--help']

  # Database dispositions are isolated from every upload tree. Only the
  # reviewed manifest is read-only and the CAS progress directory is writable.
  qdrant-source-recovery-disposition:
    image: ghcr.io/maslennikov-ig/mc-2/qdrant-operator@sha256:${QDRANT_OPERATOR_IMAGE_SHA256:?QDRANT_OPERATOR_IMAGE_SHA256 must be the 64-character release digest}
    platform: linux/amd64
    pull_policy: never
    profiles: ['operator']
    restart: 'no'
    user: '1001:1001'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      - QDRANT_API_KEY=
      - QDRANT_API_KEY_FILE=
      - QDRANT_READ_ONLY_API_KEY=
      - QDRANT_READ_ONLY_API_KEY_FILE=
      - SOURCE_RECOVERY_MANIFEST_PATH=/run/source-recovery/manifest.json
      - SOURCE_RECOVERY_JOURNAL_PATH=/run/source-recovery/progress/journal.json
    volumes:
      - ${SOURCE_RECOVERY_MANIFEST_FILE:?SOURCE_RECOVERY_MANIFEST_FILE must be set}:/run/source-recovery/manifest.json:ro
      - ${SOURCE_RECOVERY_PROGRESS_HOST_DIR:?SOURCE_RECOVERY_PROGRESS_HOST_DIR must be set}:/run/source-recovery/progress:rw
    tmpfs:
      - /tmp:size=32m,mode=1777
      - /run/qdrant-operator:size=1m,mode=0700,uid=1001,gid=1001
    networks:
      - megacampus
    command: ['source-recovery', '--help']

  # Authenticated Qdrant and textfile metrics collection.
  prometheus:
    image: prom/prometheus:v3.13.1@sha256:3c42b892cf723fa54d2f262c37a0e1f80aa8c8ddb1da7b9b0df9455a35a7f893
    platform: linux/amd64
    container_name: megacampus-prometheus
    restart: unless-stopped
    user: '65534:65534'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - '127.0.0.1:9090:9090'
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=15d
      - --storage.tsdb.retention.size=5GB
    volumes:
      - ./ops/qdrant/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./ops/qdrant/prometheus/alerts.yml:/etc/prometheus/alerts.yml:ro
      - prometheus-data:/prometheus
    secrets:
      - source: prometheus_qdrant_read_only_api_key
        target: qdrant_read_only_api_key
    tmpfs:
      - /tmp:size=16m,mode=1777
    depends_on:
      qdrant:
        condition: service_healthy
      node_exporter:
        condition: service_started
      alertmanager:
        condition: service_started
    networks:
      - megacampus
    logging:
      driver: 'json-file'
      options:
        max-size: '20m'
        max-file: '3'

  # Machine-scoped batch/application metrics only; no host collectors or host namespaces.
  node_exporter:
    image: prom/node-exporter:v1.12.0@sha256:9b0ade5e607f9dbedb0a8e11151b6011ae5bd79304c261804cfdd2cadf200a80
    platform: linux/amd64
    container_name: megacampus-qdrant-textfile-exporter
    restart: unless-stopped
    user: '65534:65534'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    command:
      - --collector.disable-defaults
      - --collector.textfile
      - --collector.textfile.directory=/var/lib/node_exporter/textfile_collector
      - --web.listen-address=:9100
    volumes:
      - ${QDRANT_METRICS_TEXTFILE_HOST_DIR:?QDRANT_METRICS_TEXTFILE_HOST_DIR must be set}:/var/lib/node_exporter/textfile_collector:ro
    networks:
      - megacampus
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  alertmanager:
    image: prom/alertmanager:v0.33.1@sha256:9e082985f56f4c8c9f724e18f2288c6708f472e56a5286b8863d080434ea065d
    platform: linux/amd64
    container_name: megacampus-alertmanager
    restart: unless-stopped
    user: '65534:65534'
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - '127.0.0.1:9093:9093'
    command:
      - --config.file=/etc/alertmanager/alertmanager.yml
      - --storage.path=/alertmanager
      - --cluster.listen-address=
    volumes:
      - ./ops/qdrant/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager-data:/alertmanager
    secrets:
      - alertmanager_telegram_bot_token
      - alertmanager_telegram_chat_id
    tmpfs:
      - /tmp:size=16m,mode=1777
    networks:
      - megacampus
    logging:
      driver: 'json-file'
      options:
        max-size: '20m'
        max-file: '3'

  grafana:
    image: grafana/grafana:12.4.5@sha256:26b8f35a9e4e4431995cf64c3f396505a4faf17bcfc19f9ed84943ec6bfd5ecd
    platform: linux/amd64
    container_name: megacampus-grafana
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - '127.0.0.1:3005:3000'
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD__FILE=/run/secrets/grafana_admin_password
      - GF_AUTH_ANONYMOUS_ENABLED=false
      - GF_PUBLIC_DASHBOARDS_ENABLED=false
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_ANALYTICS_REPORTING_ENABLED=false
      - GF_ANALYTICS_CHECK_FOR_UPDATES=false
    volumes:
      - grafana-data:/var/lib/grafana
      - ./ops/qdrant/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./ops/qdrant/grafana/dashboards:/var/lib/grafana/dashboards:ro
    secrets:
      - grafana_admin_password
    tmpfs:
      - /tmp:size=32m,mode=1777
    depends_on:
      prometheus:
        condition: service_started
    networks:
      - megacampus
    logging:
      driver: 'json-file'
      options:
        max-size: '20m'
        max-file: '3'

  # Docling MCP Server for document processing (internal - behind nginx proxy)
  # NOTE: Image built manually on server (too large for GitHub Actions)
  docling-mcp-internal:
    image: ghcr.io/maslennikov-ig/mc-2/docling-mcp:latest
    pull_policy: if_not_present # Don't pull if local image exists (too large: 8GB)
    container_name: megacampus-docling-mcp-internal
    restart: unless-stopped
    # No external ports - only accessible via nginx proxy
    volumes:
      - ./data/uploads:/app/uploads:ro
      - ./data/uploads-dev:/app/uploads-dev:ro
      - docling-models:/app/models
      - docling-cache:/app/cache
      # Shared JSON cache - worker reads from here
      - docling-json-cache:/usr/local/lib/python3.12/site-packages/_cache
    environment:
      - DOCLING_CACHE_DIR=/app/cache
      - DOCLING_MODELS_PATH=/app/models
      - DOCLING_LOG_LEVEL=INFO
      - MCP_TRANSPORT=streamable-http
      - MCP_HOST=0.0.0.0
      - MCP_PORT=8000
      - PYTHONUNBUFFERED=1
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'python -c "import socket; s=socket.socket(); s.connect((''localhost'', 8000)); s.close()" || exit 1',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    networks:
      - megacampus
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          memory: 2G
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '3'

  # Nginx proxy for Docling MCP - rewrites Host header to bypass DNS rebinding protection
  docling-mcp:
    image: nginx:alpine
    container_name: megacampus-docling-mcp
    restart: unless-stopped
    ports:
      - '127.0.0.1:8000:8000'
    volumes:
      - ./nginx-docling-proxy.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      docling-mcp-internal:
        condition: service_healthy
    # No healthcheck needed - docling-mcp-internal has its own healthcheck
    # and nginx is just a transparent proxy
    networks:
      - megacampus
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

  # NotebookLM Bridge for Stage 7 NLM audio/video generation
  notebooklm-bridge:
    image: ghcr.io/maslennikov-ig/mc-2/notebooklm-bridge:latest
    container_name: megacampus-notebooklm-bridge
    restart: unless-stopped
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      # NOTEBOOKLM_AUTH_JSON passed via env_file when set (must NOT be empty string)
      - NOTEBOOKLM_STORAGE_PATH=${NOTEBOOKLM_STORAGE_PATH:-/app/secrets/notebooklm/storage_state.json}
      - NOTEBOOKLM_HOME=${NOTEBOOKLM_HOME:-/app/secrets/notebooklm}
      - NOTEBOOKLM_GENERATION_TIMEOUT_SECONDS=${NOTEBOOKLM_GENERATION_TIMEOUT_SECONDS:-10800}
      - NOTEBOOKLM_QUEUE_WAIT_TIMEOUT_SECONDS=${NOTEBOOKLM_QUEUE_WAIT_TIMEOUT_SECONDS:-259200}
      - NOTEBOOKLM_HTTP_TIMEOUT_SECONDS=${NOTEBOOKLM_HTTP_TIMEOUT_SECONDS:-60}
      - NOTEBOOKLM_POLL_ERROR_RETRY_LIMIT=${NOTEBOOKLM_POLL_ERROR_RETRY_LIMIT:-12}
      - NOTEBOOKLM_LOG_LEVEL=${NOTEBOOKLM_LOG_LEVEL:-INFO}
      # Route NotebookLM traffic through bobabuh SOCKS5 proxy (geo-bypass)
      - HTTPS_PROXY=socks5h://172.19.0.1:1080
      - HTTP_PROXY=socks5h://172.19.0.1:1080
    volumes:
      - ${NOTEBOOKLM_STORAGE_STATE_DIR:-./secrets/notebooklm}:/app/secrets/notebooklm:ro
    healthcheck:
      test:
        [
          'CMD-SHELL',
          'python -c "import socket; s=socket.socket(); s.connect((''localhost'', 8000)); s.close()" || exit 1',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - megacampus
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '3'

  # BullMQ Worker for background jobs (course generation stages 1-6)
  worker:
    image: ghcr.io/maslennikov-ig/mc-2/api:latest
    container_name: megacampus-worker
    restart: unless-stopped
    user: '1001:1001'
    group_add:
      - '${QDRANT_METRICS_GID:?QDRANT_METRICS_GID must be a numeric dedicated group ID}'
    command: ['tsx', 'dist/orchestrator/worker-entrypoint.js']
    # Worker doesn't expose HTTP - disable healthcheck inherited from api image
    healthcheck:
      disable: true
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      - NODE_ENV=production
      - REDIS_URL=redis://megacampus-redis:6379
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_METRICS_TEXTFILE_DIR=/var/lib/megacampus/qdrant-metrics
      - QDRANT_METRICS_SERVICE=worker
      - QDRANT_METRICS_INSTANCE=worker
      - DOCLING_MCP_URL=http://docling-mcp:8000/mcp
      # Path to shared docling JSON cache
      - DOCLING_CACHE_PATH=/app/docling-cache
      # LKG config persistence
      - LKG_CONFIG_PATH=/app/data/lkg-config.json
    volumes:
      - ./data/uploads:/app/uploads
      - ${QDRANT_METRICS_TEXTFILE_HOST_DIR:?QDRANT_METRICS_TEXTFILE_HOST_DIR must be set}:/var/lib/megacampus/qdrant-metrics
      # Shared JSON cache from docling-mcp
      - docling-json-cache:/app/docling-cache
      # LKG config persistence
      - worker-data:/app/data
    networks:
      - megacampus
    depends_on:
      qdrant:
        condition: service_healthy
      redis:
        condition: service_healthy
      docling-mcp-internal:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '3'

  # Stage 7 Enrichment Worker (cover images, audio, video, quiz, presentation)
  worker-stage7:
    image: ghcr.io/maslennikov-ig/mc-2/api:latest
    container_name: megacampus-worker-stage7
    restart: unless-stopped
    command: ['tsx', 'dist/stages/stage7-enrichments/worker-entrypoint.js']
    healthcheck:
      disable: true
    env_file:
      - ${PRODUCTION_ENV_FILE:-.env.production}
    environment:
      - NODE_ENV=production
      - REDIS_URL=redis://megacampus-redis:6379
      # Use local filesystem storage (Supabase bucket was removed 30.01.2026)
      - USE_LOCAL_STORAGE=true
      - ENRICHMENTS_LOCAL_PATH=/app/data/enrichments
      - ENRICHMENTS_PUBLIC_URL=/storage/enrichments
      - NOTEBOOKLM_BRIDGE_URL=http://megacampus-notebooklm-bridge:8000
      - NOTEBOOKLM_BRIDGE_TIMEOUT_MS=${NOTEBOOKLM_BRIDGE_TIMEOUT_MS:-3600000}
    volumes:
      - ./data/uploads:/app/uploads
      - ./data/enrichments:/app/data/enrichments
    networks:
      - megacampus
    depends_on:
      redis:
        condition: service_healthy
      notebooklm-bridge:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
    logging:
      driver: 'json-file'
      options:
        max-size: '50m'
        max-file: '3'

networks:
  megacampus:
    driver: bridge
    name: megacampus-network

volumes:
  qdrant-data:
    name: megacampus_qdrant
  prometheus-data:
    name: megacampus_prometheus
  grafana-data:
    name: megacampus_grafana
  alertmanager-data:
    name: megacampus_alertmanager
  redis-data:
    driver: local
  docling-models:
    driver: local
  docling-cache:
    driver: local
  # Shared volume for docling JSON output - written by docling-mcp, read by worker
  docling-json-cache:
    driver: local
  # Worker data persistence for LKG config
  worker-data:
    driver: local

secrets:
  qdrant_api_key:
    file: ${QDRANT_API_KEY_FILE:-./secrets/qdrant_api_key}
  qdrant_read_only_api_key:
    file: ${QDRANT_READ_ONLY_API_KEY_FILE:-./secrets/qdrant_read_only_api_key}
  prometheus_qdrant_read_only_api_key:
    file: ${PROMETHEUS_QDRANT_READ_ONLY_API_KEY_FILE:-./secrets/prometheus_qdrant_read_only_api_key}
  grafana_admin_password:
    file: ${GRAFANA_ADMIN_PASSWORD_FILE:-./secrets/grafana_admin_password}
  alertmanager_telegram_bot_token:
    file: ${ALERTMANAGER_TELEGRAM_BOT_TOKEN_FILE:-./secrets/alertmanager_telegram_bot_token}
  alertmanager_telegram_chat_id:
    file: ${ALERTMANAGER_TELEGRAM_CHAT_ID_FILE:-./secrets/alertmanager_telegram_chat_id}
  qdrant_snapshot_manifest:
    file: ${QDRANT_SNAPSHOT_MANIFEST_FILE:-/var/lib/megacampus-qdrant-recovery/manifests/latest-manifest.json}
  qdrant_recovery_probe:
    file: ${QDRANT_RECOVERY_PROBE_FILE:-./recovery/probe.json}
